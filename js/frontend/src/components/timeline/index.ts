/**
 * Timeline component exports
 * 
 * Interactive timeline component for displaying session data
 * using the GSTC (Gantt Schedule Timeline Calendar) library
 */

export { TimelineView } from './TimelineView';
export { TimelineViewDemo } from './TimelineViewDemo';

export type {
  TimelineData,
  TimelineRow,
  TimelineItem,
  TimelineColumn,
  TimelineFilter,
} from './TimelineView';