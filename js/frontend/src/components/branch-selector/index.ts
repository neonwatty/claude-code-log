/**
 * Branch selector component module
 * 
 * Exports the BranchPointSelector component and related functionality
 * for selecting branch points within session message history.
 */

export { BranchPointSelector } from './BranchPointSelector';
export type { BranchPoint } from '../types/session-types';